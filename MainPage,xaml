<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LAB_3.MainPage"
             Title="Диспетчер Аспірантури">

    <Grid RowDefinitions="Auto, Auto, *, Auto" Padding="10" RowSpacing="10">
        <SearchBar Grid.Row="0" Placeholder="Пошук за ПІБ або кафедрою..." Text="{Binding SearchText}" />

        <Grid Grid.Row="1" ColumnDefinitions="2*, 1*, 1*" BackgroundColor="#eeeeee" Padding="5">
            <Label Text="ПІБ" FontAttributes="Bold" Grid.Column="0"/>
            <Label Text="Кафедра" FontAttributes="Bold" Grid.Column="1"/>
            <Label Text="Дата" FontAttributes="Bold" Grid.Column="2"/>
        </Grid>

        <CollectionView Grid.Row="2" ItemsSource="{Binding FilteredData}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid ColumnDefinitions="2*, 1*, 1*" Padding="5">
                        <Label Text="{Binding FullName}" Grid.Column="0"/>
                        <Label Text="{Binding Department}" Grid.Column="1"/>
                        <Label Text="{Binding StartDate, StringFormat='{0:dd.MM.yyyy}'}" Grid.Column="2"/>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <FlexLayout Grid.Row="3" Wrap="Wrap" JustifyContent="Center" AlignItems="Center">
            <Button Text="Відкрити" Command="{Binding OpenFileCommand}" Margin="5"/>
            <Button Text="Зберегти" Command="{Binding SaveFileCommand}" Margin="5"/>
            <Button Text="Додати" Command="{Binding AddCommand}" Margin="5"/>
            <Button Text="Про програму" Command="{Binding ShowAboutCommand}" Margin="5" BackgroundColor="Gray"/>
        </FlexLayout>
    </Grid>
</ContentPage>
